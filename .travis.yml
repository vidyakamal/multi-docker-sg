sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t vidyaaprabha/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true vidyaaprabha/react-test npm test

after_success:
  - docker build -t vidyaaprabha/multi-client ./client
  - docker build -t vidyaaprabha/multi-nginx ./nginx
  - docker build -t vidyaaprabha/multi-server ./server
  - docker build -t vidyaaprabha/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push vidyaaprabha/multi-client
  - docker push vidyaaprabha/multi-nginx
  - docker push vidyaaprabha/multi-server
  - docker push vidyaaprabha/multi-worker
